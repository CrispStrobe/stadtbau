name: stadtbau_sim
description: Pure Dart, deterministic simulation core for the Stadtbau game (no Flutter).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
